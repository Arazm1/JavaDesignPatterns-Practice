## Builder Pattern

Design a system to build different types of Cars:

ElectricCar
GasCar

Requirements:

Use a Builder interface
Include steps like:
buildEngine()
buildWheels()
buildBattery() / buildFuelTank()

Include a Director class