# Practice Task: Data Processing System
## Scenario

You are building a system that processes different types of data files.

All files follow the same processing algorithm:

Open the file

Process the data

Close the file

However, different file types process data differently:

CSV files

JSON files

The algorithm stays the same, but the processing step changes.